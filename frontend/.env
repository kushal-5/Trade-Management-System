# Dev: leave empty — browser calls `/api/*`, Vite proxies to localhost:3000 (avoids CORS).
# Or set full API origin, e.g. http://localhost:3000 (ensure api ALLOWED_ORIGIN includes your frontend URL).
VITE_BASE_URL=
VITE_API_KEY=replace_with_alpha_vantage_key
